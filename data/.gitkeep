Runtime data lives here — the Kokoro voice model, the SQLite memory database,
the ChromaDB knowledge store, OAuth caches and logs.

Everything in this directory is gitignored. It is all either downloaded on first
run, generated by Ted as you use him, or personal. Nothing here needs to be
backed up to get Ted running on another machine; the directory refills itself.
